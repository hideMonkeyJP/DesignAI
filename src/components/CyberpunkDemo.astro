---
---

<div class="cyberpunk-demo">
  <div class="glitch-container">
    <h1 class="glitch" data-text="CYBER">CYBER</h1>
    <h1 class="glitch" data-text="PUNK">PUNK</h1>
  </div>

  <div class="grid-container">
    <div class="cyber-card">
      <div class="card-content">
        <div class="neon-text">01</div>
        <h3>NEURAL</h3>
        <p>ネオンが輝く未来都市</p>
      </div>
      <div class="card-glitch"></div>
    </div>

    <div class="cyber-card">
      <div class="card-content">
        <div class="neon-text">02</div>
        <h3>DIGITAL</h3>
        <p>デジタルな世界観</p>
      </div>
      <div class="card-glitch"></div>
    </div>

    <div class="cyber-card">
      <div class="card-content">
        <div class="neon-text">03</div>
        <h3>FUTURE</h3>
        <p>未来的なインターフェース</p>
      </div>
      <div class="card-glitch"></div>
    </div>
  </div>

  <button class="cyber-button">
    <span>ENTER</span>
    <div class="button-glitch"></div>
  </button>
</div>

<style>
  .cyberpunk-demo {
    background: #0a0a0a;
    color: #fff;
    min-height: 400px;
    padding: 2rem;
    position: relative;
    overflow: hidden;
  }

  .cyberpunk-demo::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, #ff00ff1a 25%, transparent 25%),
                linear-gradient(-45deg, #00ffff1a 25%, transparent 25%);
    background-size: 4px 4px;
    pointer-events: none;
  }

  .glitch-container {
    text-align: center;
    margin-bottom: 3rem;
  }

  .glitch {
    font-size: 3rem;
    font-weight: bold;
    text-transform: uppercase;
    position: relative;
    text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
                 0.025em 0.04em 0 #fffc00;
    animation: glitch 725ms infinite;
    margin: 0;
  }

  .glitch span {
    position: absolute;
    top: 0;
    left: 0;
  }

  @keyframes glitch {
    0% {
      text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
                   0.025em 0.04em 0 #fffc00;
    }
    15% {
      text-shadow: 0.05em 0 0 #00fffc, -0.03em -0.04em 0 #fc00ff,
                   0.025em 0.04em 0 #fffc00;
    }
    16% {
      text-shadow: -0.05em -0.025em 0 #00fffc, 0.025em 0.035em 0 #fc00ff,
                   -0.05em -0.05em 0 #fffc00;
    }
    49% {
      text-shadow: -0.05em -0.025em 0 #00fffc, 0.025em 0.035em 0 #fc00ff,
                   -0.05em -0.05em 0 #fffc00;
    }
    50% {
      text-shadow: 0.05em 0.035em 0 #00fffc, 0.03em 0 0 #fc00ff,
                   0 -0.04em 0 #fffc00;
    }
    99% {
      text-shadow: 0.05em 0.035em 0 #00fffc, 0.03em 0 0 #fc00ff,
                   0 -0.04em 0 #fffc00;
    }
    100% {
      text-shadow: -0.05em 0 0 #00fffc, -0.025em -0.04em 0 #fc00ff,
                   -0.04em -0.025em 0 #fffc00;
    }
  }

  .grid-container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .cyber-card {
    background: #1a1a1a;
    border: 1px solid #00ffff;
    padding: 1.5rem;
    position: relative;
    overflow: hidden;
  }

  .cyber-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(0, 255, 255, 0.2),
      transparent
    );
    animation: shine 3s infinite;
  }

  @keyframes shine {
    0% { left: -100%; }
    20% { left: 100%; }
    100% { left: 100%; }
  }

  .neon-text {
    color: #00ffff;
    font-size: 2rem;
    margin-bottom: 1rem;
    text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 15px #00ffff;
  }

  .cyber-card h3 {
    color: #fff;
    margin-bottom: 0.5rem;
  }

  .cyber-card p {
    color: #888;
  }

  .cyber-button {
    background: none;
    border: 2px solid #ff00ff;
    color: #ff00ff;
    padding: 1rem 3rem;
    font-size: 1.2rem;
    position: relative;
    cursor: pointer;
    display: block;
    margin: 0 auto;
    overflow: hidden;
    transition: all 0.3s ease;
  }

  .cyber-button:hover {
    background: #ff00ff;
    color: #000;
    text-shadow: 0 0 5px #fff;
  }

  .cyber-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 0, 255, 0.4),
      transparent
    );
    animation: shine 2s infinite;
  }

  @media (max-width: 768px) {
    .grid-container {
      grid-template-columns: 1fr;
    }

    .glitch {
      font-size: 2rem;
    }
  }
</style>