---
---

<div class="handdrawn-demo">
  <header class="demo-header">
    <div class="logo">
      <svg class="hand-drawn-logo" viewBox="0 0 100 40">
        <path d="M10,30 Q20,10 30,30 T50,30" class="sketch-path" />
        <path d="M60,10 L70,30 L80,10" class="sketch-path" />
      </svg>
    </div>
    <nav class="nav">
      <a href="#" class="nav-link">About</a>
      <a href="#" class="nav-link">Gallery</a>
      <a href="#" class="nav-link">Contact</a>
    </nav>
  </header>

  <div class="content">
    <div class="text-content">
      <h2 class="hand-title">手描き風デザイン</h2>
      <p class="hand-text">親しみやすく、温かみのある雰囲気を演出します</p>
      <button class="hand-button">もっと見る →</button>
    </div>
    <div class="illustration">
      <svg class="hand-drawn-illustration" viewBox="0 0 200 200">
        <circle cx="100" cy="100" r="80" class="sketch-element" />
        <path d="M60,100 Q100,140 140,100" class="sketch-element" />
        <circle cx="70" cy="80" r="10" class="sketch-element" />
        <circle cx="130" cy="80" r="10" class="sketch-element" />
      </svg>
    </div>
  </div>
</div>

<style>
  .handdrawn-demo {
    background: #fff9f0;
    font-family: "Comic Sans MS", cursive;
    padding: 2rem;
    height: 100%;
    min-height: 400px;
  }

  .demo-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
  }

  .hand-drawn-logo {
    width: 120px;
    height: 40px;
  }

  .nav {
    display: flex;
    gap: 2rem;
  }

  .nav-link {
    color: #4a4a4a;
    text-decoration: none;
    font-size: 1.1rem;
    position: relative;
    transition: transform 0.2s ease;
  }

  .nav-link:hover {
    transform: scale(1.1);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 2px;
    background: #4a4a4a;
    transform: scaleX(0);
    transition: transform 0.3s ease;
    transform-origin: right;
  }

  .nav-link:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
  }

  .text-content {
    padding: 2rem;
  }

  .hand-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #4a4a4a;
    transform: rotate(-2deg);
  }

  .hand-text {
    font-size: 1.2rem;
    color: #666;
    margin-bottom: 2rem;
    line-height: 1.6;
    transform: rotate(1deg);
  }

  .hand-button {
    background: none;
    border: 2px solid #4a4a4a;
    padding: 0.8rem 2rem;
    font-size: 1.1rem;
    font-family: inherit;
    cursor: pointer;
    transform: rotate(-1deg);
    transition: all 0.3s ease;
  }

  .hand-button:hover {
    transform: rotate(1deg) scale(1.05);
    background: #4a4a4a;
    color: #fff9f0;
  }

  .illustration {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hand-drawn-illustration {
    width: 100%;
    max-width: 300px;
    height: auto;
  }

  .sketch-path,
  .sketch-element {
    fill: none;
    stroke: #4a4a4a;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  @media (max-width: 768px) {
    .nav {
      display: none;
    }

    .content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .text-content {
      padding: 1rem;
    }
  }
</style>